package geometry;

/**
 * Generalize any enumerator describing
 * polygon types: triangle (equilateral, isosceles, scalene), rectangle (square, rhomboid) etc
 */
public interface PolygonType {
    String toString();
}
